import std.array;
import std.algorithm;
import std.ascii;
import std.bigint;
import std.conv;
import std.container;
import std.complex;
import std.cstream;
import std.functional;
import std.math;
import std.numeric;
import std.random;
import std.stdio;
import std.stream;
import std.string;

int main() {
    int n;
    readf("%d",&n);

    foreach(i;0..n) {
        auto j = uniform(0,15);

    }
    return 0;
}
